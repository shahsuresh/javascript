// const sum = (num1, num2) => num1 + num2;
// const result = sum(10, 30);
// console.log(result);

//Write an arrow function that prints average of three numbers

const calculateAverage = (
  n1,
  n2,
  n3 //{
) =>
  // let sum = n1 + n2 + n3;
  //let avg = (n1 + n2 + n3) / 3;
  (n1 + n2 + n3) / 3;
//return avg;
//};

let result = calculateAverage(10, 20, 30);
console.log(result);
