//2.Check if input variable is a number or not
let inputData = "25";
console.log(typeof inputData);
if (typeof inputData === `number`) {
  console.log(`The input data is number`);
} else {
  console.log(`Input data is not a Number`);
}
